import type {ScrollViewProps} from 'react-native'

import React from 'react'
import {ScrollView} from 'react-native'

function JSScrollView(props: ScrollViewProps) {
  return <ScrollView {...props} />
}

export default JSScrollView

